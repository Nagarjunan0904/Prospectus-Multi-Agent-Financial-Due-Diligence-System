# --- Orchestration ---
langgraph==1.2.7
langgraph-checkpoint-postgres==3.1.0   # PostgresSaver checkpointer — separate package
langchain==1.3.11
langchain-openai==1.3.3                # GPT-4.1-mini as orchestrator + memo writer LLM

# --- MCP ---
mcp>=1.28.1,<2                          # v2 is alpha-only; pin <2 or a future update breaks you

# --- Sentiment (FinBERT) ---
transformers==5.12.1
# torch installed separately — see below, do NOT list it here

# --- HTTP / API ---
httpx==0.28.1
beautifulsoup4==4.15.0
fastapi[standard]==0.136.1
uvicorn[standard]==0.49.0

# --- Database ---
sqlalchemy==2.0.51
psycopg2-binary==2.9.12        # SQLAlchemy ORM (shared/db.py)
psycopg[binary,pool]>=3.1,<4          # AsyncPostgresSaver (langgraph-checkpoint-postgres uses psycopg3)

# --- Structured outputs / validation ---
instructor==1.15.4
pydantic==2.13.4

# --- Utilities ---
python-dotenv==1.2.2
pytest==9.1.1